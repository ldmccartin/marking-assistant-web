name: tsbuild
on: [push]
jobs:
  yarn-and-tsc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: install node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      - name: yarn install
        run: yarn install && yarn build
